This is a list of all the settings stored, and their defaults

Authentication
    email
        email used for puush account.
    password
        NOT STORED. This is used once to get an api key.
    key
        The auth key given by puush on successfull authentication.

Puush settings
    quality 90

Local save settings
    local-save-enabled true
    local-save-path "~/Pictures"
        Path to save files in.
    local-save-name "Screenshot-%D-%T"
        Name to save screenshots as.

